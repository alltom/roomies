{
  "message" : "You owe <%= @resident.name %> <%= number_to_currency([0, -current_resident.balance_with(@resident)].max) %>.",
  "amount" : <%= [0, -current_resident.balance_with(@resident)].max %>
}
